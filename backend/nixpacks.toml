[phases.setup]
nixPkgs = ["python312", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[start]
cmd = "python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
```

---

## Fix 3 — Make sure these files exist in `backend/`

**`backend/Procfile`** (no extension)
```
web: python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2
```

**`backend/runtime.txt`**
```
python-3.12.0